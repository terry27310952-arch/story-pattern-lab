# Brand Assets

Place the Observer identity reference at:

```text
assets/brand/observer_reference.png
```

The application treats this path as the immutable brand reference for The
Observer. The reasoning layer may vary pose, shot, camera, position, scale and
scene, but must not change the faceless black-suit identity constraints.
